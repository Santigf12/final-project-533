# etc/catalog/hive.properties
connector.name = hive
hive.metastore = file
hive.metastore.catalog.dir = local:///data/trino

# native local-FS support (this is the only flag you need)
fs.native-local.enabled = true

# root of the lake – THREE slashes
local.location = /data/trino
